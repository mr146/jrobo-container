package mainpackage;

import one.implementation.IInterface;
import one.implementation.Implementation;

import org.jboss.weld.environment.se.Weld;
import org.jboss.weld.environment.se.WeldContainer;

public class Main {
	private WeldContainer container = new Weld().initialize();
	public static void main(String[] args) {
		(new Main()).run();
	}
	private void run() {
		Implementation x = (Implementation) container.instance().select(IInterface.class).get();
		x.action();
	}
}
